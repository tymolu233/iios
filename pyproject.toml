[project]
name = "iios-cloakbrowser-setup"
version = "0.1.0"
description = "uv-managed CloakBrowser smoke tests for later iios.fun automation"
readme = "README.local.md"
requires-python = ">=3.13"
dependencies = [
    "cloakbrowser",
]

[tool.uv]
package = false
