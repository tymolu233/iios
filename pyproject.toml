[project]
name = "iios-protocol-signin"
version = "0.2.0"
description = "Pure-protocol daily sign-in for iios (WASM + urllib)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
